<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.phpprogrammist.counters">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".screens.settings.SettingsActivity"
            android:label="@string/title_activity_settings_electro"
            android:parentActivityName=".screens.records.RecordsListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ru.phpprogrammist.counters.screens.records.RecordsListActivity" />
        </activity>
        <activity android:name=".screens.record.RecordActivity" />
        <activity android:name=".screens.records.RecordsListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>